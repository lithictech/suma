export default function FundingLinkBankAccount() {
  return (
    <div>
      This component has not been rebuilt after the v2 redesign. Once we support bank
      accounts properly, it can be rebuild. Check git history for the previous version.
    </div>
  );
}
